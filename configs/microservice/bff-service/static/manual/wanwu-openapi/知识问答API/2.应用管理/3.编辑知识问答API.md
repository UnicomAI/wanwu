# 编辑知识问答API

修改知识问答的名称、描述、图标等基本信息。模型、知识库等高级配置请使用[配置更新API](2.配置更新API.md)。

## 请求接口

| Type | Instructions |
| ---- | ---- |
| 方法 | Http |
| 请求URL | 按照应用实际API根地址，例如：`http://localhost:8081/service/api/openapi/v1/rag` |
| 字符编码 | UTF-8 |
| 请求类型 | PUT |
| 鉴权方式(header参数) | `Authorization: Bearer {API Key}` |
| 请求格式 | `Content-Type: application/json` |
| 响应格式 | `Content-Type: application/json` |

## 请求参数

| Parameter | Required | Type | Instructions |
| --------- | -------- | ---- | ---- |
| uuid | 是 | string | 知识问答UUID |
| name | 是 | string | 知识问答名称 |
| desc | 否 | string | 知识问答描述 |

## 响应参数

| Parameter | Type | Instructions |
| --------- | ---- | ---- |
| code | int | 状态码，0表示成功 |
| msg | string | 提示信息 |
| data | object | 空 |

## 调用示例

```shell
curl -k --location --request PUT 'http://localhost:8081/service/api/openapi/v1/rag' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer <API Key>' \
--data '{
    "uuid": "2004010127610351679",
    "name": "产品知识问答（新版）",
    "desc": "基于最新产品手册"
}'
```

## 响应示例

```json
{
    "code": 0,
    "msg": "",
    "data": null
}
```
